import pandas as pd

sales_data = pd.read_csv("sales_data.csv")

pivot = pd.pivot_table(
    sales_data,
    values='SaleAmount',
    index=['Region','Manager','Salesman'],
    aggfunc='sum'
)

print(pivot)
