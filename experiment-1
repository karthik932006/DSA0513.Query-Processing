import pandas as pd


df = pd.read_csv("job_history.csv")


job_count = df.groupby("emp_id").size()


employees_multiple_jobs = job_count[job_count >= 2]

print("Employees who have done two or more jobs:")
print(employees_multiple_jobs.index)
