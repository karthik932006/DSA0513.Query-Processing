import pandas as pd
import numpy as np

df = pd.DataFrame(np.random.randn(10,5),
                  columns=['A','B','C','D','E'])

print(df)

def highlight(val):
    if val < 0:
        return 'color:red'
    else:
        return 'color:black'

styled = df.style.applymap(highlight)

styled
